## test에 사용한 git 명령어 정리

### git init
로컬 폴더(내 컴퓨터에 있는 폴더) 깃 활성화

### git remote origin 주소
로컬폴더와 원격저장소(레파지토리) 연결

### git remote -v 
연결 확인

### git add .
폴더내에 있는 모든 파일 스테이징

### git commit
깃 커밋 (git commit 을 최소 한번은 해주어야 branch 생성 가능)

### git push
커밋된 파일들 원격저장소로 넘기기

### git branch 브랜치이름
브랜치이름 으로 새로운 브랜치 만들기

### git branch
현재 원격 저장소에 있는 브랜치 목록 확인

### git checkout 브랜치 이름
해당 브랜치로 이동

### 기타
#### git log
#### git status